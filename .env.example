# Copy this file to .env and set real values before running docker compose.
# The docker-compose.yml reads these via ${VAR:-fallback} syntax.
# If .env is absent, safe development defaults are used automatically.

JWT_SECRET=changeme_dev
ACCESS_TOKEN_SECRET=changeme_access_dev
REFRESH_TOKEN_SECRET=changeme_refresh_dev
