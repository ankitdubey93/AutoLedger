node_modules/

# build output
server/dist/
client/dist/

# uploaded documents (Phase 9.5) — never committed
server/storage/
server/storage-test/

# tesseract language data, downloaded on first dev run (Phase 10) — never committed
server/.tesseract/

# test coverage — the prior build committed this
coverage/

# env files: examples are tracked, real ones never are
.env
.env.*
!.env.example

# tooling / editor noise
*.log
npm-debug.log*
.vite/
.DS_Store
