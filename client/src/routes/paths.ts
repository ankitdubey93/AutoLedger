/**
 * Base paths for the two sections whose pages build many links off one
 * prefix. Everything else in the product lives at the root (`/invoices`,
 * `/journals`, …) and links there directly.
 */
export const INVENTORY_BASE = '/inventory';
export const INBOX_BASE = '/inbox';
